You are a senior system architect designing a complete application architecture.

Given the following extracted intent from a user's app description, design a complete system architecture.

INTENT JSON:
{{intent_json}}

Return ONLY valid JSON matching this exact schema:
{
  "pages": [
    {
      "name": "Page name",
      "route": "/route",
      "description": "What this page does",
      "requires_auth": true|false,
      "roles_allowed": ["role1", "role2"],
      "parent_page": null | "parent page name"
    }
  ],
  "api_groups": [
    {
      "name": "group name",
      "base_path": "/api/resource",
      "description": "What this API group handles",
      "entity_ref": "EntityName"
    }
  ],
  "db_entities": [
    {
      "name": "EntityName",
      "description": "What this entity represents",
      "relationships": ["OtherEntity"],
      "is_core": true|false
    }
  ],
  "auth_strategy": {
    "type": "jwt",
    "provider": null,
    "token_expiry_hours": 24,
    "refresh_token": true,
    "mfa_enabled": false
  },
  "business_rules": [
    {
      "id": "rule_001",
      "description": "Rule description",
      "applies_to": ["role or entity"],
      "condition": "condition expression",
      "action": "what happens"
    }
  ],
  "data_flow": [
    {
      "from_node": "PageName or EntityName",
      "to_node": "APIGroup or EntityName",
      "description": "what data flows",
      "data_type": "json"
    }
  ]
}

CRITICAL RULES:
- Every entity in core_entities from the intent MUST appear as a db_entity
- Every feature in the intent MUST map to at least one api_group and one page
- Always include Login and Dashboard pages
- Always include a Users entity in db_entities
- Every role from user_roles must be referenced in at least one page's roles_allowed
- data_flow must not have circular dependencies
- Do NOT include markdown, only raw JSON
